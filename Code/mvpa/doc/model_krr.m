%% Kernal Ridge Regression
% Train the kernal ridge regression model with linear kernal.
% The regularization parameter (λ) can be fine-tuned through 'param.lambda'.
%% Input
%       >>> param.lambda (default = power(2,-10:10)) 